class BookObject {
    constructor(title, authors, thumbnail, shelfState) {
        this.title = title;
        this.authors = authors;
        this.thumbnail = thumbnail;
        this.shelfState = shelfState; //TODO: should this be here?
    }
}

export default BookObject;